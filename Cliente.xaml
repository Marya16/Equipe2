<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Equipe2_1.ClientePage"
             Title="Cliente">

    <ContentPage.Content>
        <Grid Padding="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Search Bar -->
            <StackLayout Grid.Row="0" 
                         Orientation="Horizontal"
                         HorizontalOptions="Center">

             <Entry Placeholder="Buscar cliente..."
                    WidthRequest="200" />

             <Button Text="🔍" />

            </StackLayout>

            <!-- List View -->
            <ScrollView Grid.Row="1">

       <StackLayout>
                <Frame BorderColor="Gray" 
                       CornerRadius="10" 
                       HasShadow="False">

            <StackLayout Orientation="Horizontal" 
                         Padding="10">

                <Label Text="Josefina..."
                       HorizontalOptions="StartAndExpand" />
                <ImageButton Source="cloud_icon.png" />
                <ImageButton Source="trash_icon.png" />
        </StackLayout>
                    </Frame>
                    <!-- Repeat similar Frames for other items -->
                </StackLayout>
            </ScrollView>

            <!-- Add Button -->
            <Button Grid.Row="2" 
                    Text="+" 
                    HorizontalOptions="End"
                    VerticalOptions="End" 
                    WidthRequest="50" 
                    HeightRequest="50"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>
